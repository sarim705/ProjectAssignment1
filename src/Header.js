import React from 'react';
import { Box, Typography } from '@mui/material';

const Header = () => {
  return (
    <Box sx={{ backgroundColor: '#000', padding: '10px 20px', color: '#fff', marginBottom: 1 }}>
      <Typography variant="h6">Listening 3</Typography>
    </Box>
  );
};

export default Header;
